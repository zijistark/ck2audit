# -*- python -*-

SConscript(['pdx/SConscript'])

env = Environment()
env.Append(CXXFLAGS="-Wall -Wextra -s -O3 --std=c++17")

sources = ["main.cc"]

env.Program('test', sources, LIBS=["boost_program_options", "pdx", "boost_filesystem", "boost_system"], LIBPATH='./pdx')
